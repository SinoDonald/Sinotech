<?xml version="1.0" encoding="utf-8"?>
<!--
  app.config for Sinotech_2025 (net8.0-windows)
  
  !! 注意 !!
  .NET Core / .NET 5+ 完全不支援 <assemblyBinding> bindingRedirect。
  版本衝突應由 csproj 的 PackageReference 版本號控制，
  因此此檔案僅保留空白結構，避免舊設定干擾。
-->
<configuration>
</configuration>
